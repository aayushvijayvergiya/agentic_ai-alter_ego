# LLM API — use OpenRouter (recommended, free tier available) or OpenAI
OPENROUTER_API_KEY=your_openrouter_api_key_here  # Takes priority if set
OPENAI_API_KEY=your_openai_api_key_here          # Fallback if OpenRouter not set
MODEL_NAME=openai/gpt-oss-120b:free              # Any model slug on OpenRouter or OpenAI

# SMTP Configuration - for email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
NOTIFICATION_EMAIL=your_recipient_email@gmail.com

# Optional - for push notifications (legacy)
PUSHOVER_USER=your_pushover_user_key
PUSHOVER_TOKEN=your_pushover_app_token

# Optional - GitHub profile integration
GITHUB_USERNAME=aayushvijayvergiya
GITHUB_TOKEN=your_github_personal_access_token  # Increases rate limit from 60 to 5000 req/hr
GITHUB_CACHE_DURATION=3600  # Cache TTL in seconds (default: 3600)

# Optional - portfolio website scraping
PORTFOLIO_URL=https://your-portfolio.com
PORTFOLIO_CACHE_DURATION=3600

# Optional - HTTP settings
REQUEST_TIMEOUT=10
